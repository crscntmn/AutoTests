name: Allure Pages

on:
  workflow_call:
    inputs:
      report:
        description: Report to publish
        required: true
        type: string

      run_id:
        description: CI workflow run ID
        required: true
        type: string

permissions:
  actions: read
  contents: write
  pages: write
  id-token: write

concurrency:
  group: allure-pages-publish
  cancel-in-progress: false

jobs:
  publish:
    name: Publish ${{ inputs.report }} Allure Report
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Allure Pages data branch
        uses: actions/checkout@v4
        with:
          ref: allure-pages-data
          fetch-depth: 0

      - name: Prepare directories
        run: |
          mkdir -p selenium
          mkdir -p docker
          mkdir -p pages/selenium
          mkdir -p pages/docker

      - name: Download Selenium Allure report
        if: inputs.report == 'selenium'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          rm -rf selenium/*

          gh run download \
            "${{ inputs.run_id }}" \
            --repo "${{ github.repository }}" \
            --name selenium-allure-report \
            --dir selenium

      - name: Download Docker Allure report
        if: inputs.report == 'docker'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          rm -rf docker/*

          gh run download \
            "${{ inputs.run_id }}" \
            --repo "${{ github.repository }}" \
            --name docker-allure-report \
            --dir docker

      - name: Verify Selenium report
        run: |
          if [ ! -f selenium/index.html ]; then
            echo "Selenium report does not exist."
          else
            echo "Selenium report found."
          fi

      - name: Verify Docker report
        run: |
          if [ ! -f docker/index.html ]; then
            echo "Docker report does not exist."
          else
            echo "Docker report found."
          fi

      - name: Prepare Pages
        run: |
          rm -rf pages

          mkdir -p pages/selenium
          mkdir -p pages/docker

          cp -r selenium/. pages/selenium/ 2>/dev/null || true
          cp -r docker/. pages/docker/ 2>/dev/null || true

      - name: Create main index.html
        run: |
          cat > pages/index.html <<'EOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">

            <title>AutoTests — Allure Reports</title>

            <style>
              * {
                box-sizing: border-box;
              }

              body {
                margin: 0;
                font-family:
                  -apple-system,
                  BlinkMacSystemFont,
                  "Segoe UI",
                  Roboto,
                  Arial,
                  sans-serif;
                background: #f6f8fa;
                color: #24292f;
              }

              .container {
                max-width: 900px;
                margin: 0 auto;
                padding: 70px 24px;
              }

              h1 {
                margin: 0 0 10px;
                font-size: 36px;
              }

              .subtitle {
                margin-bottom: 45px;
                color: #57606a;
                font-size: 18px;
              }

              .reports {
                display: grid;
                grid-template-columns:
                  repeat(auto-fit, minmax(280px, 1fr));
                gap: 24px;
              }

              .report {
                display: block;
                padding: 30px;
                background: white;
                border: 1px solid #d0d7de;
                border-radius: 12px;
                text-decoration: none;
                color: inherit;
                transition:
                  transform 0.15s ease,
                  box-shadow 0.15s ease;
              }

              .report:hover {
                transform: translateY(-3px);
                box-shadow:
                  0 8px 24px rgba(140, 149, 159, 0.2);
              }

              .report-title {
                margin-bottom: 12px;
                font-size: 22px;
                font-weight: 600;
              }

              .report-description {
                color: #57606a;
                line-height: 1.5;
              }

              .footer {
                margin-top: 50px;
                color: #8c959f;
                font-size: 14px;
              }
            </style>
          </head>

          <body>
            <main class="container">

              <h1>AutoTests</h1>

              <div class="subtitle">
                Allure test reports
              </div>

              <div class="reports">

                <a class="report" href="./selenium/">
                  <div class="report-title">
                    Selenium CI
                  </div>

                  <div class="report-description">
                    Tests executed directly on GitHub Actions.
                  </div>
                </a>

                <a class="report" href="./docker/">
                  <div class="report-title">
                    Docker Selenium CI
                  </div>

                  <div class="report-description">
                    Tests executed inside Docker with
                    Selenium Standalone Chrome.
                  </div>
                </a>

              </div>

              <div class="footer">
                Generated automatically by GitHub Actions.
              </div>

            </main>
          </body>
          </html>
          EOF

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Update Allure Pages data
        run: |
          rm -rf selenium
          rm -rf docker

          mkdir -p selenium
          mkdir -p docker

          cp -r pages/selenium/. selenium/ 2>/dev/null || true
          cp -r pages/docker/. docker/ 2>/dev/null || true

          git add selenium docker

          if git diff --cached --quiet; then
            echo "No report changes."
          else
            git commit -m "Update ${{ inputs.report }} Allure report"
            git push origin allure-pages-data
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4